require 'spec_helper'

describe Relative::Time do
  it 'has a version number' do
    expect(Relative::Time::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
